{
	"name": "kjeks-e2e",
	"version": "0.0.0",
	"private": true,
	"type": "module",
	"description": "Playwright acceptance tests for Kjeks core and its add-ons: proves nothing tracks before consent, and that gated scripts/embeds activate after consent.",
	"scripts": {
		"test": "playwright test",
		"test:headed": "playwright test --headed",
		"report": "playwright show-report",
		"install:browsers": "playwright install chromium"
	},
	"devDependencies": {
		"@playwright/test": "^1.48.0"
	}
}
